import { 
  ROOT_URL,
  PNG
} from "_media/awsS3Root";

export const SETTINGS_ICON = `${ROOT_URL}/settings/settings.${PNG}`;
export const CHECK_ICON = `${ROOT_URL}/settings/check.${PNG}`;

// Volume
export const VOLUME_HIGH = `${ROOT_URL}/settings/volume/volume-high.${PNG}`;
export const VOLUME_MEDIUM = `${ROOT_URL}/settings/volume/volume-medium.${PNG}`;
export const VOLUME_LOW = `${ROOT_URL}/settings/volume/volume-low.${PNG}`;