import { ROOT_URL, PNG } from '_media/awsS3Root';

// Icons
export const READING = `${ROOT_URL}/goal-filter/reading.${PNG}`;
export const WRITING = `${ROOT_URL}/goal-filter/writing.${PNG}`;
export const MATH = `${ROOT_URL}/goal-filter/math.${PNG}`;
export const COMMUNICATION = `${ROOT_URL}/goal-filter/communication.${PNG}`;

export const goalIcons = {
  reading: READING,
  writing: WRITING,
  math: MATH,
  communication: COMMUNICATION
};
