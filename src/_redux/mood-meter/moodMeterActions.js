import { createAction } from "@reduxjs/toolkit";

export const setValence = createAction('setValence');
export const setEnergy = createAction('setEnergy');
export const clearOptions = createAction('clearOptions');